# nelocker-open — brand

> Free, open resources for getting more out of AI — curated by the team behind [NeLocker](https://nelocker.com), *Your Intelligence Vault*.

**⚠️ SCAFFOLDED, NOT DESIGNED.** Every `TODO` below is a real decision nobody has made yet.
Nothing here was invented — an invented palette presented as "the brand" is worse than a blank
one, because someone builds on it. Fill these in, then generate the assets.

---

## The idea it hangs on
TODO: one sentence. Not what it does — what it should *feel* like. Everything else follows.

## Palette
TODO. Name real hex values; the tokens live here and the code mirrors them, never the reverse.

| Token | Hex | Use |
|---|---|---|
| `primary` | TODO | |
| `surface` | TODO | |
| `ink` | TODO | Body text |
| `accent` | TODO | Used **once**, not everywhere |

**Dark mode is a design, not an inversion.** Decide what the ground and the text become; a naive
invert usually turns a considered colour into a glowing one.

## Type
TODO: a display face and a body face, chosen for this project rather than defaulted.

## What it must never look like
TODO — **and this list matters more than the palette.** It is what stops an image model reaching
for the category's clichés, and it is the longest part of every prompt that works.

Start from the estate defaults, all of which are banned everywhere:
- the purple-to-blue gradient hero
- a lone acid-green or vermilion pop on near-black
- emoji as section markers
- Inter or Space Grotesk chosen because they were safe

## The mark
TODO: an abstraction, not a picture of the thing. It must read at **40px** on a home screen and
must not depend on colour alone.

## Voice
TODO: how it talks. One line on what it never says.
